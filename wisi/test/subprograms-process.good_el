;; generated by WisiToken Wisi from ../wisi/test/subprograms.wy
;; with command line: wisi-generate.exe -v 1 --output_language Ada_Emacs --lexer Elisp --interface process
;; ../wisi/test/subprograms.wy
;;

(require 'semantic/lex)
(require 'wisi-process-parse)

(defconst subprograms-elisp-keyword-table-raw
  '(
   ("function" . FUNCTION)
   ("(" . LEFT_PAREN)
   ("procedure" . PROCEDURE)
   (")" . RIGHT_PAREN)
   ))
(defconst subprograms-elisp-token-table-raw
  '(
   ("symbol"
    (SYMBOL . "")
    )
   ))
(defconst subprograms-process-token-table
  (wisi-process-compile-tokens
   [FUNCTION
    LEFT_PAREN
    PROCEDURE
    RIGHT_PAREN
    SYMBOL
    Wisi_EOI
    wisitoken_accept
    compilation_unit
    declarations
    declaration
    subprogram
    parameter_list
    ]))

(defconst subprograms-process-action-table
  (wisi-process-compile-actions
   '(nil
     (("compilation_unit:0"
       (setq wisi-test-success t))
      )
     nil
     nil
     (("subprogram:0"
       (wisi-statement-action [1 function 2 other]))
      ("subprogram:1"
       (wisi-statement-action [1 procedure 2 other]))
      )
     (("parameter_list:0"
       (wisi-statement-action [1 open-paren 2 close-paren]))
      nil
      ("parameter_list:2"
       (wisi-statement-action [1 open-paren 3 close-paren]))
      )
     )))

(provide 'subprograms-process)
