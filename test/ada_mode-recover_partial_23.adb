-- From a partial parse during a real editing session. Used to
-- encounter "error during resume", due to a bad Language_Fixes
-- attempt. Deleted that; Minimal_Complete_Actions finds a correct
-- solution faster.

declare
   Kind       : Valid_Node_Index;
begin
   loop
      if Tree then
         declare
            Id : Identifier_Index;
         begin
            if Data then
               Found := True;

            end loop;
   Name_Ident := New_Identifier (Name);
