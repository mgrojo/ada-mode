--  Encountered "error during resume", now fixed.

--EMACS_SKIP_UNLESS:(eq ada-parser 'process)

end;
Next (I);
end loop;

if Min_Length < Ada.Containers.Count_Type'Last then
   I := Working_Set.First;
