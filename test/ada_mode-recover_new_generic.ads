-- From a real editing session. Recover deletes 'generic', so indent
-- for that line was not computed.
--EMACS_SKIP_UNLESS:(eq ada-parser 'process)
--EMACSCMD:(setq skip-recase-test t)
pragma License (Modified_GPL);
generic
