-- Recover failed with ENQUEUE_LIMIT
--
-- FIXME: matching_begin return list of token arrays, so insert '(if
-- foo' is cheaper.
--EMACS_SKIP_UNLESS: nil
procedure Ada_Mode.Recover_44
is begin
  Lex_Start_Line := Token.Line_Region.First +
    then
    Shift_Lines +
    else 0);
end Ada_Mode.Recover_44;

